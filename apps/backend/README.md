# Backend Services

Reserved for future Node.js services. **No backend applications or frameworks are implemented yet.**

When added, services will live directly under `apps/backend/<service-name>/`.

**Tags:** `scope:app`, `type:app`, `platform:node`, `domain:backend`
