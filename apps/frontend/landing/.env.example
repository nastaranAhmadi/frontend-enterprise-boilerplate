# Public site origin (canonical URLs, sitemap, Open Graph).
# Production: set to your Vercel domain, e.g. https://your-app.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:4200

# Data layer: `mock` (default) or `api`
NEXT_PUBLIC_DATA_SOURCE=mock
