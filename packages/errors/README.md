# @enterprise/errors

Unified error types and `normalizeError()` for frontend and backend.

**Exports:** `ApiError`, `ValidationError`, `UnknownError`, `normalizeError()`

**Depends on:** `@enterprise/types`

**Consumers:** `api-client`, `logger`, `env`, `auth`, apps
