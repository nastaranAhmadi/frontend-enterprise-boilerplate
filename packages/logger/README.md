# @enterprise/logger

Provider-based logging architecture. Ready for Sentry, LogRocket, Datadog, or Console — no external provider installed yet.

**Exports:** `createLogger()`, `LogProvider`, `Logger`

**Depends on:** `@enterprise/types`, `@enterprise/errors`
