# @enterprise/design-tokens

Platform-agnostic design tokens (colors, spacing, typography, radius, shadows, breakpoints, z-index, animation).

All values reference CSS custom properties for theme compatibility.

**Consumers:** `theme`, `ui`
