# @enterprise/api-client

Native Fetch API client with middleware pipeline architecture.

**Features (infrastructure):** typed request/response layer, middleware, auth middleware, retry, timeout, cancellation, unified error handling.

**No endpoints implemented.** Uses native `fetch` — not Axios.

**Depends on:** `@enterprise/types`, `@enterprise/errors`, `@enterprise/logger`
